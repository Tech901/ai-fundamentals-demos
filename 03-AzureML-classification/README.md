# Resources for AzureML classification

- `data/` contains data that was used for model training
- `classifcation-inferency.ipynb` is a Jupyter notebook that shows how to use an API endpoint to run model inference.
